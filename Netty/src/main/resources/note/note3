==========================heartBeat==========================================================================
心跳包是定时发送一段数据来确定client是否和server还存在连接，当client长时间不与server通信是server就断开连接。当断开连接后client还可以从新建立连接，
重连机制用在建立连时断开重连，也可以用于在运行时断开重连。具体方法是